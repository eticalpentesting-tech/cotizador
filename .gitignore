# NUNCA commitees .env: contiene secretos (ADMIN_PASS, tokens).
.env

# Dependencias (si alguna vez se agregan)
node_modules/

# Logs
*.log

# Cookies de sesión local (nunca a un repo público: pueden contener tokens)
cookies.txt

# Capturas/artefactos de verificación y logins locales (no van al repo)
vercel_login.txt
prod_*.png
prod_*.html
NUL
